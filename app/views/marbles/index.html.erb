<!DOCTYPE html>
<html>
  <head>
    <title>Trading Floor |<%= full_title(yield(:title)) %></title>

    <h1>Marble Trading Floor</h1>
    <strong><%= link_to 'Add Your Marble Here', new_marble_path %><strong>

        <script language="javascript"> 
          function toggle() {
          var ele = document.getElementById("toggleText");
          var text = document.getElementById("displayText");
          if(ele.style.display == "block") {
            ele.style.display = "none";
            text.innerHTML = "Show Comments";
            }
          else {
            ele.style.display = "block";
            text.innerHTML = "Hide Comments";
            }
          } 
        </script>
  </head>
  <br><br><hr style="width:65%;height:1px;background-color:#2A52BE;border:none" align='center' noshade='noshade'>
  <body>
      <% @descend_marble.each do |marble| %> 
          <div class="container"> 
            <%= image_tag marble.avatar.url(:thumb) %>
            <span style="padding: 25px"><%= link_to marble.user.user_name, othersmarbles_path(:id => marble.user.id) %></span>
            <%= marble.marble_text %>
          </div>
          <div class="container">
            <span style="padding: 10px"><%= marble.marble_name %></span>
            <span style="font-family:Verdana,sans-serif; color:blue; font-size:10px"><span style="padding: 55px">Added:&nbsp; &nbsp;<%= time_ago_in_words(marble.created_at) %>&nbsp; &nbsp;ago</span>
            <span style="padding: 15px"><%= link_to 'Add Comment (partially working)', new_comment_path(:id => marble.id) %></span>
            <span style="padding: 15px">See Comments (partially working)</span><br><br>
            <%@descend_comment.each do |comment|%>
              <span style="color:black; padding: 100px"><%= comment.user.user_name%>:<span style="padding: 25px"><%= comment.comment_text %>
              </span></span><br>
            <%end%>
          </div>
          <br><hr style="width:65%;height:1px;background-color:#2A52BE;border:none" align='center' noshade='noshade'>
      <% end %>
  </body>
  <br>
  <br>
</html>




