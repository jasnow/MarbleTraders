<!DOCTYPE html>
<html>
  <head>
    <title>Trading Floor |<%= full_title(yield(:title)) %></title>

    <h1>Marble Trading Floor</h1>
    <strong><%= link_to 'Add Your Marble Here', new_marble_path %><strong>

        <script language="javascript"> 
          function toggle() {
          var ele = document.getElementById("toggleText");
          var text = document.getElementById("displayText");
          if(ele.style.display == "block") {
            ele.style.display = "none";
            text.innerHTML = "Show Comments";
            }
          else {
            ele.style.display = "block";
            text.innerHTML = "Hide Comments";
            }
          } 
        </script>
  </head>
  <br><br><hr style="width:65%;height:1px;background-color:#2A52BE;border:none" align='center' noshade='noshade'>
  <body>
      <% @descend_marble.each do |marble| %> 
          <div class="container"> 
            <%= image_tag marble.avatar.url(:thumb) %>
            <%= link_to marble.user.user_name, othersmarbles_path(:id => marble.user.id) %>
            <%= marble.marble_text %>
          </div>
          <div class="container">
            <%= marble.marble_name %>
            <span style="font-family: 'Verdana','sans-serif';">Added:&nbsp; &nbsp;<%= time_ago_in_words(marble.created_at) %>&nbsp; &nbsp;ago
            <%= link_to 'Add Comment (partially working)', new_comment_path(:id => marble.id) %>
            See Comments (also not working!) <%=marble.id%> </span> <!-- marble.id is just for testing. -->
            <%#@descend_comment.each do |comment, marble|%>
              <%#= comment.comment_text %>
            <%#end%>
          </div>
          <br><hr style="width:65%;height:1px;background-color:#2A52BE;border:none" align='center' noshade='noshade'>
      <% end %>
  </body>
</html>




